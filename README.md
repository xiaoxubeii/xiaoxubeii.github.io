# xiaoxubeii.github.io

Personal site built with Astro and deployed to GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site builds to `docs/`. GitHub Pages is configured to publish from
the `main` branch and `/docs` folder.
